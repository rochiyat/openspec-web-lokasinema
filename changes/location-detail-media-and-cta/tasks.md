# Tasks: location-detail-media-and-cta

1. `useLocationDetail(slug)` + SWR; handle 404 slug tidak ditemukan.
2. Komponen gallery + lightbox/ carousel sesuai pola UI proyek.
3. Section specs accordion/table sesuai `LocationSpecs` PRD.
4. Implementasi `buildLocationWhatsAppMessage` + tombol link.
5. CTA web: link ke halaman/form inquiry dengan guard auth (placeholder route sampai change inquiry).
6. Related locations block (query atau field dari API).
7. SEO title/description dasar boleh stub; enhancement penuh di change SEO.
