# Tasks: location-listing-search-filter-map

1. Hook `useLocations` (SWR) dengan key dari serialisasi query params.
2. Komponen filter: keyword, city, category, tag, featured, price range, sort — sinkron URL.
3. Grid `LocationCard` sesuai field PRD.
4. Pagination atau load more + loading skeleton.
5. Integrasi Google Maps embed panel (fallback documented).
6. Empty state + error state.
7. QA: deep link dengan query lengkap, back/forward browser.
