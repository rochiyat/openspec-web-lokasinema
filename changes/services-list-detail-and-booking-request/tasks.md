# Tasks: services-list-detail-and-booking-request

1. Backend: entity Service + admin CRUD + public list/detail filters `is_published`.
2. Frontend: `/services` grid/list + `/services/[slug]` page.
3. WhatsApp CTA pada detail service.
4. Form booking request (auth) + POST inquiries `service_booking`.
5. Admin UI services list + edit.
6. Uji: unpublished hidden; guest tidak bisa submit booking web; rate limit.
